// import { Response } from "express";

// Response.prototype.sendResponse = function (status: number, message: string, data?: any): void {
//     this.status(status).json({
//         status,
//         message,
//         result: status >= 200 && status < 300,
//         data
//     });
// };